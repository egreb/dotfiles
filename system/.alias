# Shortcuts

alias reload="source ~/.zshrc"
alias _="sudo"
alias g="git"
alias rr="rm -rf"

# Global aliases

alias tree="tree -A"
alias treed="tree -d"
alias tree1="tree -d -L 1"
alias tree2="tree -d -L 2"

alias week="date +%V"
alias speedtest="wget -O /dev/null http://speed.transip.nl/100mb.bin"

# Docker
alias dcu="docker-compose up -d"
alias dcs="docker-compose stop"
alias dockerDeleteImages="docker rmi $(docker images -a -q)"

# Python
alias python="python3"
alias branchclean="branchclean.py"

# Git
# alias gp="git push -u origin $(git symbolic-ref --short HEAD)"
# alias gpf="git push -u origin $(git symbolic-ref --short HEAD) --force-with-lease"
